# Day 16 – Task Log

## Objective

To understand asynchronous programming in JavaScript and learn how to execute tasks without blocking the main thread using callback functions, promises, `async`/`await`, and timer functions.

---

## Topics Covered

* Synchronous vs Asynchronous JavaScript
* Callback Functions
* JavaScript Promises
  * Promise States
    * Pending
    * Fulfilled
    * Rejected
  * Promise Methods
    * `.then()`
    * `.catch()`
    * `.finally()`
* `async` and `await`
* Timer Functions
  * `setTimeout()`
  * `setInterval()`
  * `clearTimeout()`
  * `clearInterval()`

---

## Tasks Assigned

| Sr. No. | Task | File Name |
| ------: | ---------------------------------------------------------- | ---------------- |
| 1 | Practice callback functions and JavaScript promises | `callbacks.js` and `promises.js` |
| 2 | Implement asynchronous programming using `async` and `await` | `async-await.js` |
| 3 | Practice timer functions for delayed and repeated execution | `timers.js` |

---

## Work Completed

Successfully implemented callback functions to understand asynchronous execution and practiced creating and handling JavaScript promises using different promise states and methods. Developed asynchronous functions using `async` and `await` to simplify asynchronous code. Also implemented timer functions such as `setTimeout()` and `setInterval()` to perform delayed and repeated execution. All programs were executed and tested successfully using Visual Studio Code and the browser console.

---

## Skills Practiced

* Asynchronous JavaScript
* Callback Functions
* JavaScript Promises
* Async/Await
* Timer Functions
* Event Loop Fundamentals
* Problem Solving

---

## Learning Outcomes

* Understood the difference between synchronous and asynchronous execution.
* Learned how callback functions are used in asynchronous programming.
* Gained practical experience in creating and handling JavaScript promises.
* Learned how `async` and `await` simplify asynchronous code.
* Understood the purpose and usage of JavaScript timer functions.
* Improved confidence in writing efficient and responsive JavaScript applications.

---

## Challenges & Solutions

Initially found it challenging to understand the execution flow of asynchronous code and the differences between callbacks, promises, and `async`/`await`. Through implementing multiple examples and observing program execution, I developed a clearer understanding of how asynchronous operations are handled in JavaScript.

---

## Conclusion

Today's session introduced the fundamentals of asynchronous programming in JavaScript. Practicing callback functions, promises, `async`/`await`, and timer functions strengthened my understanding of non-blocking program execution and prepared me for working with APIs and other asynchronous operations in modern web development.