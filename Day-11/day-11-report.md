# Day 11 – Task Log

## Objective

To understand the concept of arrays in JavaScript and learn how to store, manipulate, and process collections of data using both basic and advanced array methods.

---

## Topics Covered

* Introduction to Arrays
* Creating Arrays
* Accessing and Modifying Array Elements
* Array Properties
* Basic Array Methods
  * `push()`
  * `pop()`
  * `shift()`
  * `unshift()`
  * `splice()`
  * `slice()`
  * `concat()`
  * `indexOf()`
  * `includes()`
* Advanced Array Methods
  * `forEach()`
  * `map()`
  * `filter()`
  * `find()`
  * `reduce()`

---

## Tasks Assigned

| Sr. No. | Task | File Name |
| ------: | --------------------------------------------------- | ------------------- |
| 1 | Practice array creation and basic array operations | `arrays.js` |
| 2 | Implement and test basic array methods | `array-methods.js` |
| 3 | Practice advanced array methods such as `map()` and `filter()` | `advanced-arrays.js` |
| 4 | Solve problems using `find()`, `reduce()`, and `forEach()` | `advance-methods-practice.js` |

---

## Work Completed

Successfully created and manipulated arrays using various basic array methods. Practiced adding, removing, and modifying array elements and then implemented advanced array methods such as `map()`, `filter()`, `find()`, `reduce()`, and `forEach()` to process and transform data efficiently. These exercises improved my understanding of handling collections of data in JavaScript.

---

## Skills Practiced

* JavaScript Arrays
* Array Manipulation
* Data Processing
* Problem Solving
* Logical Thinking

---

## Learning Outcomes

* Understood the concept and importance of arrays in JavaScript.
* Learned how to create, access, and modify array elements.
* Gained practical experience with commonly used array methods.
* Understood how `map()` transforms data and `filter()` selects specific elements.
* Learned to search for elements using `find()`.
* Understood how `reduce()` can combine multiple values into a single result.
* Learned to iterate through arrays using `forEach()`.
* Improved confidence in solving problems involving collections of data.

---

## Challenges & Solutions

Initially found it challenging to understand the working of callback functions and the differences between advanced array methods such as `map()`, `filter()`, and `reduce()`. Through repeated practice and implementing small examples, the purpose and usage of each method became much clearer.

---

## Conclusion

Today's session focused on arrays and their methods, which are essential for working with collections of data in JavaScript. Practicing both basic and advanced array methods strengthened my understanding of data manipulation and introduced me to functional programming concepts that are widely used in modern JavaScript development.